name: Terraform Plan

on:
  pull_request:
    branches: [main]
    paths:
      - "*.tf"
      - ".github/workflows/terraform-*.yml"

permissions:
  id-token: write # requerido para autenticar contra AWS vía OIDC
  contents: read
  pull-requests: write # para comentar el plan en el PR
  security-events: write # subir el SARIF de Checkov a la pestaña Security

concurrency:
  group: terraform-plan-${{ github.event.pull_request.number }}
  cancel-in-progress: true

# Todos los valores de "vars." son GitHub Actions repository VARIABLES
# (Settings > Secrets and variables > Actions > Variables) - no son
# secretos, son config especifica de este ambiente. Ver README.
env:
  AWS_REGION: us-east-1
  TF_VAR_owner: ${{ vars.OWNER }}
  TF_VAR_network_compute_subnet_ids: ${{ vars.NETWORK_COMPUTE_SUBNET_IDS }}
  TF_VAR_network_public_subnet_ids: ${{ vars.NETWORK_PUBLIC_SUBNET_IDS }}
  TF_VAR_dns_zone_id: ${{ vars.DNS_ZONE_ID }}

jobs:
  plan:
    name: Plan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Configurar credenciales AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          role-to-assume: arn:aws:iam::740104998573:role/eks-cluster-ci-plan
          aws-region: ${{ env.AWS_REGION }}

      # policies/*.json esta gitignored a proposito (ver CLAUDE.md) - se
      # descarga en cada run, version pineada, para que alb_controller.tf
      # pueda leerla via file().
      - name: Descargar IAM policy oficial del ALB Controller
        run: |
          curl -sSf -o policies/aws-load-balancer-controller-iam-policy.json \
            https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/v2.13.0/docs/install/iam_policy.json

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_version: "~1.16"
          terraform_wrapper: false

      - name: Terraform fmt (check)
        run: terraform fmt -check -recursive

      - name: Terraform init
        run: terraform init -input=false

      - name: Terraform validate
        run: terraform validate

      - name: Setup tflint
        uses: terraform-linters/setup-tflint@1cf010d3c7aef302051ccdb68c14c5dc2efa34ef # v6.3.1

      - name: tflint
        env:
          # sin esto, tflint --init pega contra la API pública de GitHub sin
          # autenticar (60 req/hora por IP, compartida entre todos los
          # runners de GitHub Actions) para bajar el plugin de aws - rate
          # limit real, ya nos rompió un run en azure-virtual-network el
          # 2026-09-05. Con el token del propio job sube a 5000/hora.
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          tflint --init
          tflint --recursive

      - name: Checkov
        id: checkov
        uses: bridgecrewio/checkov-action@f967808197a8d784d3e72919f38c3ff0cda7884e # v12.3122.0
        with:
          directory: .
          framework: terraform
          quiet: true
          soft_fail: false # bloqueante — findings aceptados quedan documentados con #checkov:skip inline
          output_format: cli,sarif
          output_file_path: console,results.sarif

      - name: Subir resultados de Checkov a la pestaña Security
        # correr también cuando Checkov falla (soft_fail: false) - ahí es
        # cuando más importa que quede el SARIF. Pero NO si un paso anterior
        # (tflint, validate) ya falló y Checkov ni llegó a correr.
        if: always() && steps.checkov.outcome != 'skipped'
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4.37.9
        with:
          sarif_file: results.sarif

      - name: Terraform plan
        id: plan
        shell: bash
        run: |
          set +e
          terraform plan -input=false -no-color -out=tfplan | tee plan_output.txt
          echo "exitcode=${PIPESTATUS[0]}" >> "$GITHUB_OUTPUT"
        continue-on-error: true

      - name: Comentar plan en el PR
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('plan_output.txt', 'utf8');
            const truncated = plan.length > 60000
              ? plan.slice(0, 60000) + '\n\n... (salida truncada)'
              : plan;

            const summaryMatch = plan.match(/Plan:\s*(\d+)\s*to add,\s*(\d+)\s*to change,\s*(\d+)\s*to destroy/);
            const destroyCount = summaryMatch ? parseInt(summaryMatch[3], 10) : 0;

            const warning = destroyCount > 0
              ? [
                  `> [!WARNING]`,
                  `> Este plan **destruye o reemplaza ${destroyCount} recurso${destroyCount === 1 ? '' : 's'}**. Revisar con cuidado antes de mergear.`,
                  '',
                ]
              : [];

            const body = [
              '### Terraform Plan',
              '',
              ...warning,
              '<details><summary>Ver plan</summary>',
              '',
              '```',
              truncated,
              '```',
              '',
              '</details>',
              '',
              `*Commit: ${context.sha.substring(0, 7)}*`,
            ].join('\n');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const previous = comments.find(
              (c) => c.user.type === 'Bot' && c.body.startsWith('### Terraform Plan')
            );

            if (previous) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: previous.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }

      - name: Fallar si terraform plan falló
        if: steps.plan.outputs.exitcode == '1'
        run: exit 1
